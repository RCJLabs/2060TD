{
  "name": "2060td",
  "private": true,
  "version": "1.17.0",
  "description": "2060TD — hybrid tower defense / base builder set in a modern alternate-history war",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "screenshot": "node scripts/screenshot.mjs",
    "balance": "tsx src/tools/balance.ts",
    "build:single": "SINGLE_FILE=1 vite build --outDir dist-single --emptyOutDir && node scripts/single-file.mjs"
  },
  "dependencies": {
    "phaser": "^3.85.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "@types/node": "^26.2.0",
    "eslint": "^9.15.0",
    "playwright": "^1.49.0",
    "tsx": "^4.23.12",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.15.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
